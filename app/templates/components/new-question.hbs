{{#if condition1}}

  <p>Have you slammed your head against your desk more than five times?</p>
  <button class="btn btn-primary btn-xs"{{action 'changeit2'}}>Yes</button>

{{else if condition2}}

  <p>Have you performed a ritualistic sacrifice to the Code God?</p>
  <button class="btn btn-primary btn-xs"{{action 'changeit3'}}>Yes</button>

{{else if condition3}}

  <form>
    <div class="form-group">
      <label for="name">You & Your Pair</label>
      {{input value=name id="name" placeholder="e.g. Ryan and Michael"}}
    </div>

    <div class="form-group">
      <label for="question">Question</label>
      {{input value=question id="question" placeholder="What's JavaScript?"}}
    </div>
  </form>

  <button class="btn btn-primary btn-xs"{{action 'newQuestion'}}>Submit Question</button>

{{else if condition4}}
  <p>Thanks for submitting! Help is on the way.</p>
  <button class="btn btn-primary btn-xs"{{action 'cancelQuestion'}}>Nevermind, we got it.</button>
{{else}}

  <p>Have you re-enacted the fax machine smashing scene from Office Space?</p>
  <button class="btn btn-primary btn-xs"{{action 'changeit'}}>Yes</button>
{{/if}}
